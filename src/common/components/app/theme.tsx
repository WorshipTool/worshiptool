import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#0085FF",
            dark: "#532EE7"
        },
        secondary: {
            main: "#EBBC1E"
        },
        success: {
            main: "#43a047"
        }
    }
});
